using System;

namespace exercise2_bis
{
    class Program
    {
        static void Main(string[] args)
            {int postal;string adressare;int telef;
            Console.WriteLine("Buna, poti raspunde la urmatoarele intrebari ? ");
            Console.WriteLine("y/n");string raspuns = Console.ReadLine();
            if (raspuns.ToLower() == "y"){Console.WriteLine("Ok, sa incepem!");
            while (true){Console.WriteLine("Domnul / Doamna");string adresare = Console.ReadLine();
            if (adresare.ToLower() == "domnul" || adresare.ToLower() == "doamna")
            {adressare = adresare;break;}else{continue;}}
            Console.WriteLine("Care este numele tau ? ");
            string nume = Console.ReadLine();Console.WriteLine("Care este prenumele tau ?");
            string prenume = Console.ReadLine();Console.WriteLine("Care este adresa ta ? (strada)");
            string strada = Console.ReadLine();Console.WriteLine("Ce judet ?");
            string judet = Console.ReadLine();while (true){Console.WriteLine("Codul postal ?");
            string codPostal = Console.ReadLine();bool verifica = int.TryParse(codPostal, out postal);
            if (verifica == true && codPostal.Length == 6){break;}else{continue;}}
            while (true){Console.WriteLine("Care este numarul tau de telefon ?");
            string telefon = Console.ReadLine();bool verifica2 = int.TryParse(telefon, out telef);
            if (verifica2 == true && telefon.Length == 10){break;}else{continue;}}
            Console.WriteLine("Care este adresa ta de e-mail ?");string mail = Console.ReadLine();
            for (int i = 0; i < 5; i++){Console.WriteLine(".");}
            Console.WriteLine($"Persoana: {adressare} {prenume} {nume} \n" + 
            $"Adresa: {strada}, " + "{judet}, {postal} \n" + $"Date de contact: {telef}, {mail}");}
            else{Console.WriteLine("Ok, no problem");}Console.ReadLine();
        }
    }
}